#编译

```
cd ../yig-iam
make
```

#修改yig-iam配置文件

```
mkdir /etc/yig-iam
cp .../yig-iam/config/* /etc/yig-iam/
vim /etc/yig-iam/conf.toml
确保数据库连接配置正确
```

#修改yig的配置文件
```
vim /etc/yig/yig.toml
添加以下内容

[plugins.yig_iam]
path = "/etc/yig/plugins/yig_iam_plugin.so"
enable = true
[plugins.yig_iam.args]
EndPoint="http://127.0.0.1:8080/api/v1/yig/fetchsecretkey"
ManageKey="key"
ManageSecret="secret"
```

#启动yig-iam

```
./yig-iam

```
